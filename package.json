{
  "name": "gloom-server",
  "version": "1.0.0",
  "author": "markx86",
  "license": "GPLv3",
  "dependencies": {
    "@types/express": "^5.0.1",
    "express": "^5.1.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/ws": "^8.5.14",
    "esbuild": "^0.25.0"
  },
  "scripts": {
    "_buildServer": "./scripts/build-server.sh",
    "_buildClient": "./scripts/build-client.sh",
    "build": "mkdir -p ./dist && npm run _buildClient && npm run _buildServer",
    "serve": "npm run build && ./dist/server.js"
  }
}
